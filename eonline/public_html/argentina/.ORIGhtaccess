Allow from All

# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /argentina/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /argentina/index.php [L]
</IfModule>

# END WordPress
